using UnityEngine;
public class CollisionSystem
{
	public bool IsCollision(CollisionData targetA, CollisionData targetB)
	{
		if (targetA.MyColliderType == ColliderType.Sphere)
		{

		}
		return false;
	}
}